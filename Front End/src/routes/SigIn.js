import React from "react";

export default function SigIn() {
    return (
      <main style={{ padding: "1rem 0" }}>
       
        {/* if (response.data == "authenticatedUSER"){
          localStorage.setItem("am","authenticatedUSER")
        }
        
        
        elk(response.data == "authenticatedADMIN") {
             localStorage.setItem("am","authenticatedADMIN")
          
        }

        else { setmessage("Your username or Passwrd is invalid. Please try again") }
      })
    console.log(check)

  } */}
      </main>
    );
  }